# Benchmark Instructions

This repository is used to benchmark project-scoped Codex agents.

Use only the project-scoped agents defined in this repository.

Autonomously decide which agents are useful for the requested task, when to invoke them, and in what order.

Do not rely on agent or skill definitions outside this repository.

Benchmark tasks must be executed against the repository state provided by the current branch.

