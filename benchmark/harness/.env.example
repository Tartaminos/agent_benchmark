# Copy to benchmark/harness/.env and keep the real keys local.
LANGFUSE_PUBLIC_KEY=pk-lf-REPLACE_ME
LANGFUSE_SECRET_KEY=sk-lf-REPLACE_ME
LANGFUSE_BASE_URL=http://localhost:3001

# Benchmark artifacts are intentionally written outside the benchmark repository.
BENCHMARK_RUNS_ROOT=../agent_benchmark_runs
